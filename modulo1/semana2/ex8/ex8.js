var numinicial = parseInt(window.prompt('Com que número deseja iniciar a PA:'));
var raiz = parseInt(window.prompt('Qual será a raiz da PA:'));

for (i=0; i < 10; i++) {
    window.alert(`${numinicial += raiz}`)
}