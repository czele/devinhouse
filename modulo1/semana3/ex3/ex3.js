var lista = [];
var elementolista = 1;

for (i=0; elementolista ; i++) {
    elementolista = parseInt(window.prompt('Que número quer adicionar?'));
    lista.push(elementolista)
    lista.pop()
}

console.log(lista)